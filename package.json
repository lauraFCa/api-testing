{
  "scripts": {
    "cy:open": "npx cypress open",
    "cy:api": "npx cypress run --record --spec 'cypress/e2e/api-tests/*.spec.js'"
  },
  "devDependencies": {
    "cypress": "^15.9.0",
    "wait-on": "^9.0.0"
  }
}
